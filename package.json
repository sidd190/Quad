{
  "name": "quad",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @quad/api dev",
    "dev:web": "pnpm --filter @quad/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.4.1",
      "onFail": "download"
    }
  },
  "packageManager": "pnpm@12.4.1"
}
